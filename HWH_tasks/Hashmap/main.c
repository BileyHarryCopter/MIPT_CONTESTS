#include "hashmap.h"

int main (void)
{
    FILE * file;
    hashmap hshmp = {};
    File_Open (file);

    assert (Hash_Ctor (&hshmp, INITSIZE) != NULL);

    

    File_Clse (file);
    return 0;
}
